import React from 'react';

import styles from './TierToolPage.module.scss';

const TierToolPage: React.FC = () => {
	return (<section className={styles.section}>
		in progress...
	</section>);
};

export default TierToolPage;
